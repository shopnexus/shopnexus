# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code local data
.claude/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Env / secrets
.env
.env.*
!.env.example
*.local

# Logs
*.log
logs/

# Docker / runtime volumes
volumes/
data/

# Node / build artifacts (root-level tooling only; submodules manage their own)
node_modules/
dist/
build/
.cache/
tmp/

# k8s secret env (never commit)
secret.env
deploy/k8s/base/secret.env

# Former submodule: the backend now reaches LLMs via internal/provider/llm/litellm,
# so `embedding` is no longer a component of the system. The directory may still be
# present in an older working tree — it is not tracked here.
embedding/
